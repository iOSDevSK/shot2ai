{
  "name": "html2wp-chrome-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Chrome extension: send an annotated screenshot to the html2wp app's chat",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
